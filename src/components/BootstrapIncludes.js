import React, { Component, PropTypes } from 'react';

class BootstrapIncludes extends Component {
    static propTypes = {
        className: PropTypes.string,
    };

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
            </div>
        );
    }
}

export default BootstrapIncludes;
    